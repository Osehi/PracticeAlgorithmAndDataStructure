package testlambdafunction

fun main(){
    var target = arrayListOf<Int>()
    var list = arrayListOf<Int>(9, 8, 7, 6, 5)
    /*
    list.sorted().forEach { item ->
//        println(item)
    }

     */
     var result = list.map { item ->

    }
    println(result)
}